#include "main.h"
#include <stdio.h>

/**
 * main - print the number of arguments entered
 * @argc: number of arguments
 * @argv: arguments received
 * Return: zero
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	argv = argv;

	return (0);
}
